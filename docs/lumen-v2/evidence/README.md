# Evidence

每个任务建立独立目录：

```text
evidence/<TASK-ID>/
├── commands.txt
├── test-results.txt
├── build-results.txt
├── screenshots/
└── README.md
```

只提交脱敏、可公开、可复现的证据。禁止真实客户图片、密钥、Authorization header、完整生产请求体和 base64 图片。
