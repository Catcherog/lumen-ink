# 11｜SCAN-001 GPT 审核与裁决

## 1. 验收结论

**状态：通过，带后续约束。**

扫描报告满足只读要求，提供了目录结构、组件树、编辑调用链、状态真相、Provider 能力矩阵、持久化行为、构建结果、规格冲突、文件映射和迁移风险。它已经足以结束“未知仓库结构”阶段。

本次通过不代表可以直接开始大规模 UI 重构。扫描同时暴露出工程基线不完整：

- client lint 未通过；
- 前后端没有 test 脚本；
- `PromptInput.tsx` 及四个工具面板没有完成逐行事实核对；
- 当前没有适合 3 人工作区的持久化和任务基础设施；
- 安全配置存在 P0 阻断项。

因此下一任务不是 UI-001，而是 `BASE-001`。

## 2. 扫描报告中确认的关键事实

1. 页面结构是顶栏 Provider/模型 + 左工具栏 + 中央 ResultViewer + 右 ParamPanel。
2. 编辑依赖 `/api/edit` 长同步请求，前端超时 100 秒，Seedream 超时 80 秒。
3. 图片长期真相主要是前端 base64；localStorage 只保存轻量 history。
4. Vercel 使用 `/tmp/lumen-ink-data/providers.json`，不具备可靠持久性。
5. ProviderFactory 的能力判断仍由模型字符串规则驱动。
6. 根 build、client/server build 和 typecheck 通过。
7. lint 有 2 个错误、2 个警告；前后端没有测试脚本。
8. 规格包提出的项目、任务、版本、质量保护和安全门禁均尚未实现。

## 3. 对 SCAN-001 的质量评价

| 维度 | 结论 |
|---|---|
| 只读纪律 | 通过 |
| 结构覆盖 | 通过 |
| 调用链覆盖 | 通过 |
| 数据真相识别 | 通过 |
| 构建证据 | 通过 |
| 测试基线 | 已正确识别为空 |
| 安全风险 | 通过 |
| UI 面板逐行覆盖 | 不完整，转入 BASE-001 补齐 |
| 工作量估算 | 仅有顺序，没有人日估算；当前不阻塞 |

## 4. 对实施顺序的修正

扫描报告建议直接进入 UI-001。GPT 不采纳该顺序，原因如下：

- 当前 lint 失败，后续差异无法建立干净基线；
- 当前没有测试命令，无法满足每轮完成定义；
- 未核对面板内提交按钮和参数实现，直接重构容易遗漏行为；
- 大规模 UI 改造前应先建立最小回归保护。

冻结顺序：

```text
BASE-001 → UI-001 → FLOW-001 → STORAGE-001 → VERSION-001
→ JOB-001 → ROUTING-001 → HARDEN-001
```

## 5. 当前不可接受的实现捷径

- 不得仅把 Provider 下拉框 CSS 隐藏，而不建立后续路由策略接口。
- 不得把 history 改名为 Version 就宣称版本系统完成。
- 不得用 localStorage 保存完整 base64 图片作为生产多人持久化。
- 不得用随机计时器伪造“分析中/生成中”的阶段。
- 不得保留 `changeme` 或 JWT fallback 并通过前端隐藏来规避安全问题。
- 不得在 UI-001 期间顺手重写 Provider、数据库和任务系统。
- 不得将 lint/test 失败写为“已有问题”后继续放行。

## 6. 下一验收点

`BASE-001` 只有在以下全部满足后通过：

- lint 0 error / 0 warning；
- client、server 和 root 均有可执行 test 命令；
- 最小测试真实执行并通过；
- build 和 typecheck 继续通过；
- 未改变可见产品行为；
- 补齐未扫描面板的事实表；
- 提交完整变更证据和回滚方式。
