# Trae Prompt｜JOB-001 可恢复生成任务

> 前置条件：`VERSION-001` 已通过。

## 目标

将长同步调用改为可恢复 GenerationJob：

- 创建 jobId；
- 持久化真实阶段；
- 查询状态；
- 取消和重试；
- 断线/刷新恢复；
- 标准 errorCode 和 diagnosticId；
- 成功顺序为 Asset → Version → Job succeeded；
- 失败不创建成功版本。

## 要求

- 禁止伪造百分比；
- 保持 Provider 协议兼容；
- 测试超时、额度不足、网络中断、保存失败；
- 明确 best-effort 取消边界；
- 旧 `/api/edit` 仅作为受控兼容层，提供弃用计划。
